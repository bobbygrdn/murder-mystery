import MysteryCards from "../../components/mysteryCards/MysteryCards";
import "./Cases.css";

const Cases = () => {
    return (
        <article id="cases">
            <MysteryCards />
        </article>
    );
};

export default Cases;